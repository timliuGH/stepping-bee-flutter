import 'package:flutter/material.dart';

void main() => runApp(SteppingBee());

class SteppingBee extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
